# Copy to .env and edit. deploy/run.sh sources it.

ORACLE_GPUS=0,1,2,3,4,5,6,7
ORACLE_SHARDS_PER_GPU=3
ORACLE_HOST=127.0.0.1
ORACLE_PORT=8000

ORACLE_TOKEN=

# Where the boltz source and CUDA venv live (used by deploy/run.sh).
NOVA_DIR=/root/nova
